version: "1"
name: default

thresholds:
  aggregate: 0.0

regression_checks:
  enabled: false
  baseline: prod
  max_absolute_drop: 0.02
  strict_metrics:
    - policy_compliance

approval:
  candidate_requires_all_thresholds: true
  approved_requires:
    mode: auto
