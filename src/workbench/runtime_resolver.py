"""Phase 6 — Runtime resolver. Stub.

Per SPEC §12, the contract is locked in Phase 1: `resolve(env)` takes only an
environment name and returns the active bundle, never ad-hoc bundle args.
"""
from __future__ import annotations


def resolve(env: str):  # noqa: ARG001
    raise NotImplementedError("Implemented in Phase 6")
